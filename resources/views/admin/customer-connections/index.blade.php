@extends('layouts.portal')
@section('content')
<div class="mb-7"><h1 class="text-xl font-extrabold">بررسی اتصال‌های مشتریان</h1><p class="mt-1 text-sm text-slate-500">پیش از تأیید، مالکیت شماره و تنظیمات ارائه‌دهنده را خارج از این صفحه بررسی کنید. تأیید در این صفحه به‌تنهایی به معنی ثبت موفق در FreeSWITCH نیست.</p></div>
@if (session('status'))<div class="mb-5 rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-700">{{ session('status') }}</div>@endif
@if ($errors->any())<div class="mb-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif
<section class="panel overflow-hidden"><h2 class="border-b border-slate-100 p-5 font-bold">ارائه‌دهندگان مشتری</h2><div class="overflow-x-auto"><table class="w-full min-w-[760px] text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="p-4">مشتری</th><th class="p-4">اتصال</th><th class="p-4">سرور</th><th class="p-4">روش</th><th class="p-4">وضعیت</th><th class="p-4">اقدام</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($gateways as $gateway)<tr><td class="p-4">{{ $gateway->tenant?->name }}</td><td class="p-4">{{ $gateway->display_name }}<span class="block text-xs text-slate-500">{{ $gateway->provider_name }}</span></td><td class="p-4" dir="ltr">{{ $gateway->host }}:{{ $gateway->port }}</td><td class="p-4">{{ $gateway->connection_method === 'ip' ? 'IP' : 'نام کاربری' }}</td><td class="p-4">{{ $gateway->verification_status }}</td><td class="p-4"><div class="flex gap-3">@if ($gateway->verification_status === 'pending')<form method="POST" action="{{ route('admin.customer-connections.gateways.approve', $gateway) }}">@csrf<button class="font-bold text-emerald-700">تأیید</button></form>@endif @if ($gateway->verification_status !== 'rejected')<form method="POST" action="{{ route('admin.customer-connections.gateways.reject', $gateway) }}">@csrf<button class="font-bold text-red-700">رد</button></form>@endif</div></td></tr>@empty<tr><td colspan="6" class="p-5 text-slate-500">اتصالی ثبت نشده است.</td></tr>@endforelse</tbody></table></div></section>
<section class="panel mt-7 overflow-hidden"><h2 class="border-b border-slate-100 p-5 font-bold">شماره‌های مشتری</h2><div class="overflow-x-auto"><table class="w-full min-w-[700px] text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="p-4">مشتری</th><th class="p-4">شماره</th><th class="p-4">ارائه‌دهنده</th><th class="p-4">وضعیت</th><th class="p-4">اقدام</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($numbers as $number)<tr><td class="p-4">{{ $number->tenant?->name }}</td><td class="p-4" dir="ltr">{{ $number->number }}</td><td class="p-4">{{ $number->providerGateway?->display_name }}</td><td class="p-4">{{ $number->statusLabel() }}</td><td class="p-4"><div class="flex gap-3">@if ($number->status === 'pending')<form method="POST" action="{{ route('admin.customer-connections.numbers.approve', $number) }}">@csrf<button class="font-bold text-emerald-700">تأیید</button></form><form method="POST" action="{{ route('admin.customer-connections.numbers.reject', $number) }}">@csrf<button class="font-bold text-red-700">رد</button></form>@endif</div></td></tr>@empty<tr><td colspan="5" class="p-5 text-slate-500">شماره‌ای ثبت نشده است.</td></tr>@endforelse</tbody></table></div></section>
@endsection
